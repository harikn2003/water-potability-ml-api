version: "3.9"

services:
  ml-api:
    build: .
    container_name: water_ml_api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    restart: always
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload